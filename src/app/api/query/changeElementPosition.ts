interface IChangeElementPositionPayload {
  activePath: string;
  overPath: string;
  scope: string;
}

const changeElementPosition = ({activePath, overPath}: IChangeElementPositionPayload) => {};

export default changeElementPosition;
